-- =====================================================================
-- 민선 MES·QMS — v9 마이그레이션
--   회의록(260814 생산 4.3) 반영: 품목 대표불량 사진 (POP 작업시작 시 표시)
--   Supabase SQL Editor 에서 실행하세요. (anon 키로는 DDL 실행 불가)
-- =====================================================================
alter table items add column if not exists defect_photos text;  -- 대표불량 사진 [{url,note}] JSON (최대 4매)
